window.addEventListener('load', () => {
    alert(`Die Website wird nicht funktionieren wenn sie nciht auf einen Server lauft.
    Wenn python installiert ist kann man einen Webserver mit (python -m SimpleHTTPServer {Port}) anstarten.
    Es wird auch mit jedem anderen Webserver funktionieren.
    `);
});